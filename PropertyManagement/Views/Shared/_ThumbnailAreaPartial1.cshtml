@model PropertyManagement.Models.ThumbnailBoxViewModel

<div class="row">
    @if (Model.Thumbnails1 != null)
    {
        foreach (var thumbnail in Model.Thumbnails1)
        {
            <div class="col-xs-6 col-md-3">
                <div class="thumbnail">
                    <div class="thumbnail-img-container">
                        <a href="@thumbnail.Link"><img class="thumbnail-img" src="@thumbnail.ImageUrl" alt="" /></a>
                    </div>
                    <div class="caption">
                        <h3><a href="@thumbnail.Link">@thumbnail.Address</a></h3>
                        <p>@Html.Raw(thumbnail.RentalPrice)</p>
                    </div>
                </div>
            </div>
        }
    }
</div>
